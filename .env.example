# NanoClaw Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# =============================================================================
# REQUIRED - Telegram Bot
# =============================================================================

# Bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Your numeric Telegram user ID (get it from @userinfobot)
TELEGRAM_OWNER_ID=

# =============================================================================
# AI Provider Keys (passed into agent containers)
# =============================================================================

# Claude auth (choose one, or leave both empty to auto-detect from keychain):
#   Option 1: Standard API key from console.anthropic.com
#   ANTHROPIC_API_KEY=sk-ant-api03-...
#   Option 2: OAuth token (if you know what you're doing)
#   CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...
#   Option 3 (default): Leave empty — auto-reads from macOS keychain
#     (uses Claude Code's own OAuth token, refreshed automatically)

# OpenAI API key — used for Whisper transcription AND as the provider key
# when groups are configured with provider: 'openai'
OPENAI_API_KEY=

# Firecrawl API key for web scraping
FIRECRAWL_API_KEY=

# Supermemory API key for long-term memory
# SUPERMEMORY_API_KEY=

DEBUG_THREADS=false

# =============================================================================
# AI Provider Defaults
# =============================================================================

# Default AI provider for groups without explicit providerConfig
# Options: anthropic, openai (default: anthropic)
# DEFAULT_PROVIDER=anthropic

# Default model override — passed to the provider's SDK
# Examples:
#   Anthropic: claude-sonnet-4-5-20250929, claude-opus-4-20250514, claude-haiku-4-5-20251001
#   OpenAI: gpt-4o, o3, gpt-4o-mini
# Leave empty to use each provider's default model
# DEFAULT_MODEL=
# =============================================================================
# OPTIONAL - Customization
# =============================================================================

# Bot trigger name (default: Andy) - messages starting with @Name trigger the bot
# ASSISTANT_NAME=Andy

# Docker image for agent containers (default: nanoclaw-agent:latest)
# CONTAINER_IMAGE=nanoclaw-agent:latest

# Container execution timeout in ms (default: 300000 = 5 min)
# CONTAINER_TIMEOUT=300000

# Max container output size in bytes (default: 10485760 = 10MB)
# CONTAINER_MAX_OUTPUT_SIZE=10485760

# Sandbox idle timeout in ms (default: 1800000 = 30 min)
# SANDBOX_IDLE_TIMEOUT_MS=1800000

# Set to false to always use 127.0.0.1 for sandbox URLs in wait-for-user messages
# SANDBOX_TAILSCALE_ENABLED=true

# CUA desktop sandbox Docker config
# CUA_SANDBOX_IMAGE=trycua/cua-xfce:latest
# CUA_SANDBOX_PLATFORM=linux/amd64
# CUA_SANDBOX_COMMAND_PORT=8000
# CUA_SANDBOX_VNC_PORT=5901
# CUA_SANDBOX_NOVNC_PORT=6901
# CUA_SANDBOX_SCREEN_WIDTH=1024
# CUA_SANDBOX_SCREEN_HEIGHT=768
# CUA_SANDBOX_SCREEN_DEPTH=24
# CUA_SANDBOX_SHM_SIZE=512m
# Optional: enables CUA server-side computer-use-agent features
# CUA_API_KEY=

# Log level: trace, debug, info, warn, error, fatal (default: info)
# LOG_LEVEL=info

# Timezone for scheduled tasks (default: system timezone)
# TZ=America/New_York
